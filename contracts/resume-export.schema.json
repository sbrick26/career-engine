{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "resume-export",
  "description": "The ONLY artifact career-engine hands to consumers (the portfolio site today). The engine owns facts, ranking, and writing; consumers own rendering. Every claim inside traces to a fact row in the private career hub per the honesty contract.",
  "type": "object",
  "required": ["version", "generated", "resume"],
  "properties": {
    "version": { "type": "integer", "minimum": 1 },
    "generated": { "type": "string", "description": "ISO timestamp of the writing run" },
    "resume": {
      "type": "object",
      "required": ["summary", "experience", "education"],
      "properties": {
        "summary": { "type": "string", "minLength": 1 },
        "experience": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/entry" } },
        "education": { "type": "array", "items": { "$ref": "#/$defs/entry" } }
      }
    },
    "skills": { "type": "array", "description": "optional: skill groups when the run updates them" },
    "projects": { "type": "array", "description": "optional: project cards when the run updates them" }
  },
  "$defs": {
    "entry": {
      "type": "object",
      "required": ["title", "points"],
      "properties": {
        "title": { "type": "string", "minLength": 1 },
        "org": { "type": "string" },
        "when": { "type": "string" },
        "points": { "type": "array", "items": { "type": "string", "maxLength": 220 } }
      }
    }
  }
}
